<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:background="@android:color/transparent"
    android:layout_width="match_parent"
    android:layout_height="@dimen/tool_bar_height"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <FrameLayout
        android:id="@+id/chatFrameLayout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintEnd_toStartOf="@id/searchFrameLayout">
        <ImageView
            android:src="@drawable/ic_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/dp8"
            android:contentDescription="@null"
            android:layout_gravity="center_vertical"/>
    </FrameLayout>

    <FrameLayout
        android:id="@+id/searchFrameLayout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintEnd_toStartOf="@id/moreFrameLayout">
        <ImageView
            android:src="@drawable/ic_search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp8"
            android:contentDescription="@null"
            android:layout_gravity="center_vertical"/>
    </FrameLayout>
    <FrameLayout
        android:id="@+id/moreFrameLayout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">
        <ImageView
            android:contentDescription="@null"
            android:padding="5dp"
            android:src="@drawable/ic_more"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="@dimen/layout_margin_4dp"
            android:layout_marginEnd="@dimen/layout_margin_8dp"/>
    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>